import*as THREE from'./node_modules/three/src/Three.js';var scene,camera,renderer,geometry,material,mesh;window.noRun||(init(),animate());function init(){scene=new THREE.Scene,camera=new THREE.PerspectiveCamera(75,window.innerWidth/window.innerHeight,1,1e4),camera.position.z=1e3,geometry=new THREE.BoxGeometry(200,200,200),material=new THREE.MeshBasicMaterial({color:16711680,wireframe:!0}),mesh=new THREE.Mesh(geometry,material),scene.add(mesh),renderer=new THREE.WebGLRenderer,renderer.setSize(window.innerWidth,window.innerHeight),document.body.appendChild(renderer.domElement)}function animate(){requestAnimationFrame(animate),mesh.rotation.x+=0.01,mesh.rotation.y+=0.02,renderer.render(scene,camera)}